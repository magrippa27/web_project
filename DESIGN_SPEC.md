# Design spec (paste your Figma values here)

When you have your Figma details, update the `@theme` block in `src/index.css` with these values so the whole app stays consistent.

## Colors (hex)
- Primary: (e.g. #3b82f6)
- Primary hover: (e.g. #2563eb)
- Background: (e.g. #f8fafc)
- Surface / cards: (e.g. #ffffff)
- Text: (e.g. #1e293b)
- Text muted: (e.g. #64748b)

## Typography
- Font family: (e.g. Inter, system-ui)
- Heading size / weight
- Body size / weight

## Spacing & radius
- Card padding, gap between sections
- Border radius (buttons, cards, inputs)

## Icons
- Export SVGs from Figma (right-click → Copy as SVG) and add under `src/components/icons/` or inline in components.
